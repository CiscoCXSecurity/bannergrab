TODO
====

The plan is to do the following in future versions:

  * Allow the user to choose a trigger to send
  * Allow the user to select the default trigger
  * Implement more triggers

Any other ideas, attach to a message and stick it on the
Source Forge project site.
